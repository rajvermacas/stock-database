interface:
  display_name: "Talk to Stock Data"
  short_description: "Query and acquire local stock market data"
  default_prompt: "Use $talk-to-stock-data to acquire missing requested stock data when needed, then analyze local prices and indicators."
